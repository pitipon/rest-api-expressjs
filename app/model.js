import db from '../db'

const Model = {
    findAll() {
        return db[this.key]
    },

    find(id) {

    },

    create(attrs) {

    },

    update(id, attrs) {

    },

    destroy(id) {

    }
}

export default Model